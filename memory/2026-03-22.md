# 2026-03-22 — 0Latency Launch Prep + Gmail Integration

## Evening Session (~20:16-20:30 UTC)
- Justin sent vectorized logo SVG (VTracer auto-trace, 1MB) — saved to site/logos/0latency-logo-final.svg
- Generated PNGs at all sizes (512, 256, 128, 64, 32, 16px + 1200x630 OG)
- Justin noted it doesn't hold up at small sizes — auto-trace vector is too noisy for clean favicon
- **Deployed all site files** from site/ to /var/www/0latency/ via rsync
- **Restarted API** with MEMORY_DB_CONN env var — now fully healthy (624 memories, Redis connected)
- Added MEMORY_DB_CONN to /root/.bashrc for persistence
- All auth routes confirmed live: /auth/github, /auth/github/callback, /auth/google, /auth/google/callback, /auth/email/register, /auth/email/login, /auth/me, /auth/api-key/regenerate
- GitHub OAuth redirect working (307 to github.com/login/oauth/authorize)
- All site pages verified: landing, login, support, blog, integrations, robots.txt, sitemap.xml, llms.txt — all 200
- www.0latency.ai CNAME still needed in Cloudflare (nginx already configured for redirect)
- Justin went to lunch, told me to keep working and called out response latency (5 min gap — queue issue during processing)

## Major Work Done

### 0Latency Product (memory-product)
- **SEO optimization** — meta tags, Open Graph, Twitter Cards, JSON-LD structured data, sitemap.xml, robots.txt added to landing page
- **llms.txt + llms-full.txt** — AI crawler discoverability files created
- **Support/FAQ page** — support.html with 7 FAQ items, contact cards, status page link
- **Blog post** — "We Lost 36% of Our Agent's Most Important Work" at site/blog/why-your-agent-forgets.html
- **GitHub README** — polished, developer-facing, with badges, comparison tables, code examples
- **Integration pages** — 4 framework-specific landing pages (OpenClaw, LangChain, CrewAI, AutoGen) + index at site/integrations/
- **Logo concepts** — SVG + PNG: icon, full wordmark, wordmark-only, favicon, OG image
- **Color rebrand** — changed from indigo/purple (#818cf8) to orange (#f97316) across ALL 13 site files + logos
- **Auth system built** — GitHub OAuth + Google OAuth + email/password login
  - Auth routes in api/auth.py
  - Users table (memory_service.users) with migration 003
  - JWT tokens (72hr, stored in /root/credentials/jwt_secret.key)
  - Login page (site/login.html) matching orange theme
  - Auto-onboarding: new users get tenant + API key on first login
  - Existing API key auth untouched
- **Database migration** — users table deployed to Supabase

### Gmail Integration
- Google Cloud project "OpenClaw" — Gmail API enabled
- OAuth credentials stored at /root/credentials/gmail_oauth_client.json
- Token stored at /root/credentials/gmail_token.json (refresh token: YES)
- Connected: jghiglia@gmail.com (387K messages)
- Can read, search, send, modify

### DNS / Infrastructure
- 0latency.ai DNS confirmed pointing to 164.90.156.169 via Cloudflare (proxied)
- api.0latency.ai also configured
- SSL handled by Cloudflare — no certbot needed
- https://0latency.ai serving 200
- https://api.0latency.ai/health returning OK (624 memories, Redis connected)

### GitHub OAuth
- Justin created OAuth app on GitHub
- Credentials stored at /root/credentials/github_oauth.json
- Client ID: Ov23liM9v81DG5ktuqxV

## Key Decisions
- **Orange branding** — Justin wants to differentiate from the purple/indigo AI startup default
- **GitHub OAuth as primary** — developer audience expects it
- **Google + email as secondary** — matching Mem0's login options
- **Open-source strategy** — open-source the core engine, sell the hosted service (Supabase/PostHog model)
- **GitHub stars as key metric** — citing OpenClaw's growth as the model

## Late Evening Session (~21:10-21:41 UTC) — Philosophy Update + Stripe + Obsidian

### "It Just Works" Philosophy Codified
- Justin's directive: "Zero latency is the underlying promise, overarching claim, and everything"
- Nobody configures latency tolerance — it just works. Steve Jobs philosophy.
- API becomes: Memory(api_key), .add(), .recall() — three lines, no config knobs
- Three architectural rules codified:
  1. Recall = always synchronous, always fast (sub-100ms)
  2. Extraction = accept instantly, process in background, never block
  3. Partial results beat blocking
- **Sub-agent deployed philosophy update** across entire codebase:
  - All site pages updated (hero, FAQ, blog, all 5 integration pages)
  - README rewritten with Philosophy section
  - ROADMAP updated — async extraction is architectural principle, not feature toggle
  - New API endpoints: POST /memories/extract (202 Accepted + job_id), GET /memories/extract/{job_id}
  - All deployed to /var/www/0latency/ and verified

### Stripe Setup — New Account
- Justin's old Stripe account was Startup Smartup — LLC is dissolved, bankrupt
- Advised: create new Stripe account as sole proprietor, not modify old one
- Brand name: **0Latency** (no space) — statement descriptor: **0LATENCY**
- Used `jghiglia+0latency@gmail.com` for new Stripe account (gmail + trick)
- Justin was going through Stripe registration at session end
- Still need: API keys (sk_live, pk_live) + webhook secret stored via SSH on server
- Webhook endpoint: https://api.0latency.ai/billing/webhook
- Events: checkout.session.completed, customer.subscription.created/deleted, invoice.paid/payment_failed

### Main Thread Responsiveness — Working
- Justin explicitly praised the sub-agent pattern: "Nice work on automatically having the sub agent do the work so you can stay here, present with me."
- The Main Thread Responsiveness Rule is paying off. Keep doing this.

### Obsidian + Claude Code Video Analysis
- Video: https://youtu.be/6MBq1paspVU (Greg Eisenberg + Internet Vin)
- Obsidian = markdown vault with backlinks, used as "second brain" for Claude Code
- Custom slash commands (/context, /ideas, /trace, /connect, /graduate) scan vault for insights
- **Key gap we fill:** Obsidian is manual (human writes → organizes → prompts). 0Latency is automatic (agent talks → memories auto-extracted → agent recalls instantly).
- **Marketing angle:** "Obsidian is your second brain. 0Latency is your agent's."
- **Integration opportunity:** Import Obsidian vault into 0Latency — one more integration page
- **Validating quote:** "The quality of information the agent has entirely determines what it can do for you."
- Sent full analysis to Justin via Telegram. Asked if he wants Obsidian integration page built.

### Obsidian CLI (new tool from Obsidian)
- Allows Claude Code to read vault files AND backlink/relationship data
- This is what makes the vault more than just a folder — the graph relationships
- Pain point from video: requests take much longer because reading so many files — exactly what 0Latency solves with indexed recall

## Still Pending
- **Stripe:** Justin creating new account — need API keys + webhook secret stored via SSH
- Google OAuth redirect URI needs "Web application" type credential (current is Desktop)
- Logo: Justin reviewing 4 geometric SVG concepts — no selection yet
- www CNAME in Cloudflare still needed
- Obsidian integration page (pending Justin's response)
- Update site messaging to "zero memory latency" where not yet done by sub-agent

## Communication Rule Added
- **NEVER ask Justin to paste API keys/secrets into Telegram chat.** Always provide terminal/SSH instructions. (He called this out — twice now.)

## Justin's Waterbar Schedule This Week
- Sun 3/22: 5:00 PM BT
- Mon 3/23: 4:00 PM BT BAR
- Tue 3/24: 3:30 PM LG BAR
- Wed 3/25: 3:30 PM UPSTRS
- Thu 3/26: 3:00 PM
- Fri/Sat: Off

## Video Watched
- Greg Eisenberg + "Boring Marketer" (James) — SEO for local/boring businesses using Claude Code
- Key takeaway: foundational SEO = LLM discoverability. No separate GEO strategy needed. llms.txt is a bonus.
- Use-case pages are the developer-tool equivalent of location pages
