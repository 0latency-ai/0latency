# First Tweet Options — @0latencyai

## Option 1: The Builder Announcement

Your AI agent forgets everything between sessions. We fixed that.

0Latency — persistent memory for AI agents:
• Sub-100ms recall
• Structured extraction (facts, preferences, decisions)
• Knowledge graphs
• 3 lines of code

Free tier. Open source. Ship it.

→ pip install 0latency
→ https://0latency.ai

## Option 2: The Problem Statement

Every AI agent builder eventually writes the same code: "how do I make my agent remember things?"

We built the answer. 0Latency is a memory layer API.

Store memories. Recall them in 12ms. Temporal decay. Contradiction detection. Zero config.

pip install 0latency
https://0latency.ai

## Option 3: The Minimalist

Built an agent memory API.

- `mem.add("user prefers Python")` → extracted, stored
- `mem.recall("schedule a code review")` → 12ms, relevant context returned

No vector DB setup. No config. No tuning.

https://0latency.ai
